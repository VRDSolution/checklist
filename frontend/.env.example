# Frontend environment example
# In dev, if omitted, frontend uses /api/v1 and Vite proxy to localhost:8000.

# Use only when calling a custom API host directly:
# VITE_API_BASE_URL=http://localhost:8000/api/v1